<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <div class="navbar">
      <div class="logo">SETU<br /><span>TECHNOLOGIES INC</span></div>
      <div class="nav-links">
        <div class="dropdown">
          <button class="dropbtn">Dashboard</button>
          <div class="dropdown-content">
            <a href="#">Assets</a>
            <a href="#">Sensors</a>
            <a href="#">Features</a>
            <a href="#">Time Frames</a>
          </div>
        </div>
        <% if (user) { %>
        <a href="/logout" class="logout">Logout</a>
        <% } else { %>
        <a href="/login">Login</a>
        <% } %>
      </div>
    </div>
    <div class="content"><%= body %></div>
  </body>
</html>
